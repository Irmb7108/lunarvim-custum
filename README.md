# NvChad
nvim --version

# Git
git --version

# Node.js و npm 
node --version
npm --version
```
# Download config.lua
yay -S node npm
# Clone LunarVim
git clone https://github.com/NvChad/starter ~/.config/nvim && nvim
```
